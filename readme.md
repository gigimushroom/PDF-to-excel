Read V2 PDFs, parse the keywords, append to new CSV file.

For HUAXIA.

## Install
```
pip install virtualenv

virtualenv myproject source myproject/venv/bin/activate

pip -r requirements.txt
```

### how to dump dep
```
pip3 install pipreqs
5680  pipreqs . --force
```